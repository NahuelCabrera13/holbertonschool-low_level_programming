#include"main.h"

/**
 * _abs - reconoce los caracteres en minuscula
 * @n: es la variable aignada
 * Return: 1 es si todo esta bien, 0 si no,
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
