#include <stdio.h>

/**
 * main - Imprime los números del 0 al 9 en base 10
 *
 * Return: Siempre 0 (Éxito)
 */
int main(void)
{
	int num;

	for (num = 0; num < 10; num++)
		putchar(num);

	putchar(num);
	return (0);
}
