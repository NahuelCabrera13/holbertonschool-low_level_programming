#include <stdio.h>

/**
 * main - use el comando printf
 *
 * Return: 0 el codigo esta correcto
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
